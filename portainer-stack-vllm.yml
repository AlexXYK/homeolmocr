version: '3.8'

services:
  # VLLM Server - Runs the model efficiently
  vllm-server:
    image: vllm/vllm-openai:latest
    container_name: olmocr-vllm-server
    command: >
      --model allenai/olmOCR-2-7B-1025-FP8
      --port 8000
      --gpu-memory-utilization 0.90
      --max-model-len 8192
      --trust-remote-code
    ports:
      - "8000:8000"
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - HF_HOME=/root/.cache/huggingface
    volumes:
      - vllm_cache:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 180s
    networks:
      - olmocr-network

  # FastAPI Wrapper - Your API on port 5005
  olmocr-api:
    image: alexxyk/olmocr-api:vllm
    container_name: olmocr-api
    ports:
      - "5005:5005"
    environment:
      - PORT=5005
      - VLLM_BASE_URL=http://vllm-server:8000/v1
      - MODEL_NAME=allenai/olmOCR-2-7B-1025-FP8
      - TARGET_IMAGE_DIM=1024
    depends_on:
      - vllm-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5005/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - olmocr-network

volumes:
  vllm_cache:
    driver: local

networks:
  olmocr-network:
    driver: bridge

